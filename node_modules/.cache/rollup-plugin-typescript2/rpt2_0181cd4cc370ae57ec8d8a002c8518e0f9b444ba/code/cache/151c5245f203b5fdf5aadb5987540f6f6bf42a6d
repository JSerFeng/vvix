{"code":"export const patch = (curr, old) => {\r\n};\r\nexport const patchProps = (instance, newProps, oldProps) => {\r\n    for (const key in newProps) {\r\n        const next = newProps[key];\r\n        const prev = oldProps[key];\r\n        if (next !== prev) {\r\n        }\r\n    }\r\n};\r\n","references":["D:/PROJECT/vix/lib/vdom/vdom.ts"]}
